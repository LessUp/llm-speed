diff --git a/cuda_llm_ops/bindings.cpp b/cuda_llm_ops/bindings.cpp
index a3258b5..d8a8ad7 100644
--- a/cuda_llm_ops/bindings.cpp
+++ b/cuda_llm_ops/bindings.cpp
@@ -1,3 +1,4 @@
+#include <cuda_bf16.h>
 #include <cuda_fp16.h>
 #include <cuda_runtime.h>
 #include <pybind11/numpy.h>
@@ -22,6 +23,17 @@ void naive_attention_fp32(const float*,
                           cudaStream_t);
 void naive_attention_fp16(
     const half*, const half*, const half*, half*, int, int, int, int, float, bool, cudaStream_t);
+void naive_attention_bf16(const __nv_bfloat16*,
+                          const __nv_bfloat16*,
+                          const __nv_bfloat16*,
+                          __nv_bfloat16*,
+                          int,
+                          int,
+                          int,
+                          int,
+                          float,
+                          bool,
+                          cudaStream_t);
 void tiled_attention_fp32(const float*,
                           const float*,
                           const float*,
@@ -35,6 +47,17 @@ void tiled_attention_fp32(const float*,
                           cudaStream_t);
 void tiled_attention_fp16(
     const half*, const half*, const half*, half*, int, int, int, int, float, bool, cudaStream_t);
+void tiled_attention_bf16(const __nv_bfloat16*,
+                          const __nv_bfloat16*,
+                          const __nv_bfloat16*,
+                          __nv_bfloat16*,
+                          int,
+                          int,
+                          int,
+                          int,
+                          float,
+                          bool,
+                          cudaStream_t);
 void flash_attention_fp32(const float*,
                           const float*,
                           const float*,
@@ -48,6 +71,17 @@ void flash_attention_fp32(const float*,
                           cudaStream_t);
 void flash_attention_fp16(
     const half*, const half*, const half*, half*, int, int, int, int, float, bool, cudaStream_t);
+void flash_attention_bf16(const __nv_bfloat16*,
+                          const __nv_bfloat16*,
+                          const __nv_bfloat16*,
+                          __nv_bfloat16*,
+                          int,
+                          int,
+                          int,
+                          int,
+                          float,
+                          bool,
+                          cudaStream_t);
 void tensor_core_gemm_fp16(
     const half*, const half*, float*, int, int, int, float, float, cudaStream_t);
 void hgemm_fp32(const float*,
@@ -94,11 +128,21 @@ void validate_attention_inputs(const torch::Tensor& q,
     TORCH_CHECK(v.is_contiguous(), "V must be contiguous");
 
     auto dtype = q.scalar_type();
-    TORCH_CHECK(dtype == torch::kFloat32 || dtype == torch::kFloat16,
-                "Only float32 and float16 are supported");
+    TORCH_CHECK(dtype == torch::kFloat32 || dtype == torch::kFloat16 || dtype == torch::kBFloat16,
+                "Only float32, float16, and bfloat16 are supported");
     TORCH_CHECK(k.scalar_type() == dtype, "K must have same dtype as Q");
     TORCH_CHECK(v.scalar_type() == dtype, "V must have same dtype as Q");
 
+    if (dtype == torch::kBFloat16) {
+        int device_idx = q.device().index();
+        int major = 0;
+        cudaDeviceGetAttribute(&major, cudaDevAttrComputeCapabilityMajor, device_idx);
+        TORCH_CHECK(major >= 8,
+                    "bfloat16 attention requires SM80+ hardware (got SM",
+                    major,
+                    "x). Use float16 or float32 on this device.");
+    }
+
     // Validate tensor dimensions are positive
     TORCH_CHECK(q.size(0) > 0 && q.size(1) > 0 && q.size(2) > 0 && q.size(3) > 0,
                 "Tensor dimensions must be positive (non-zero)");
@@ -159,7 +203,7 @@ torch::Tensor naive_attention(const torch::Tensor& q,
                              scale,
                              is_causal,
                              stream);
-    } else {
+    } else if (q.scalar_type() == torch::kFloat16) {
         naive_attention_fp16(reinterpret_cast<const half*>(q.data_ptr<at::Half>()),
                              reinterpret_cast<const half*>(k.data_ptr<at::Half>()),
                              reinterpret_cast<const half*>(v.data_ptr<at::Half>()),
@@ -171,6 +215,19 @@ torch::Tensor naive_attention(const torch::Tensor& q,
                              scale,
                              is_causal,
                              stream);
+    } else {
+        naive_attention_bf16(
+            reinterpret_cast<const __nv_bfloat16*>(q.data_ptr<at::BFloat16>()),
+            reinterpret_cast<const __nv_bfloat16*>(k.data_ptr<at::BFloat16>()),
+            reinterpret_cast<const __nv_bfloat16*>(v.data_ptr<at::BFloat16>()),
+            reinterpret_cast<__nv_bfloat16*>(output.data_ptr<at::BFloat16>()),
+            batch_size,
+            num_heads,
+            seq_len,
+            head_dim,
+            scale,
+            is_causal,
+            stream);
     }
 
     return output;
@@ -208,7 +265,7 @@ torch::Tensor tiled_attention(const torch::Tensor& q,
                              scale,
                              is_causal,
                              stream);
-    } else {
+    } else if (q.scalar_type() == torch::kFloat16) {
         tiled_attention_fp16(reinterpret_cast<const half*>(q.data_ptr<at::Half>()),
                              reinterpret_cast<const half*>(k.data_ptr<at::Half>()),
                              reinterpret_cast<const half*>(v.data_ptr<at::Half>()),
@@ -220,6 +277,19 @@ torch::Tensor tiled_attention(const torch::Tensor& q,
                              scale,
                              is_causal,
                              stream);
+    } else {
+        tiled_attention_bf16(
+            reinterpret_cast<const __nv_bfloat16*>(q.data_ptr<at::BFloat16>()),
+            reinterpret_cast<const __nv_bfloat16*>(k.data_ptr<at::BFloat16>()),
+            reinterpret_cast<const __nv_bfloat16*>(v.data_ptr<at::BFloat16>()),
+            reinterpret_cast<__nv_bfloat16*>(output.data_ptr<at::BFloat16>()),
+            batch_size,
+            num_heads,
+            seq_len,
+            head_dim,
+            scale,
+            is_causal,
+            stream);
     }
 
     return output;
@@ -257,7 +327,7 @@ torch::Tensor flash_attention(const torch::Tensor& q,
                              scale,
                              is_causal,
                              stream);
-    } else {
+    } else if (q.scalar_type() == torch::kFloat16) {
         flash_attention_fp16(reinterpret_cast<const half*>(q.data_ptr<at::Half>()),
                              reinterpret_cast<const half*>(k.data_ptr<at::Half>()),
                              reinterpret_cast<const half*>(v.data_ptr<at::Half>()),
@@ -269,6 +339,19 @@ torch::Tensor flash_attention(const torch::Tensor& q,
                              scale,
                              is_causal,
                              stream);
+    } else {
+        flash_attention_bf16(
+            reinterpret_cast<const __nv_bfloat16*>(q.data_ptr<at::BFloat16>()),
+            reinterpret_cast<const __nv_bfloat16*>(k.data_ptr<at::BFloat16>()),
+            reinterpret_cast<const __nv_bfloat16*>(v.data_ptr<at::BFloat16>()),
+            reinterpret_cast<__nv_bfloat16*>(output.data_ptr<at::BFloat16>()),
+            batch_size,
+            num_heads,
+            seq_len,
+            head_dim,
+            scale,
+            is_causal,
+            stream);
     }
 
     return output;
diff --git a/src/flash_attention.cu b/src/flash_attention.cu
index 4f4b800..cc4b997 100644
--- a/src/flash_attention.cu
+++ b/src/flash_attention.cu
@@ -1,5 +1,8 @@
 #include <cfloat>
 
+#include <cuda_bf16.h>
+#include <cuda_fp16.h>
+
 #include "common.cuh"
 #include "online_softmax.cuh"
 #include "pipeline.cuh"
@@ -328,3 +331,45 @@ void flash_attention_fp16(const half* Q,
     CUDA_CHECK (cudaGetLastError())
         ;
 }
+
+void flash_attention_bf16(const __nv_bfloat16* Q,
+                          const __nv_bfloat16* K,
+                          const __nv_bfloat16* V,
+                          __nv_bfloat16* O,
+                          int batch_size,
+                          int num_heads,
+                          int seq_len,
+                          int head_dim,
+                          float scale,
+                          bool is_causal,
+                          cudaStream_t stream) {
+    constexpr int BLOCK_M = 32;
+    constexpr int BLOCK_N = 32;
+
+    dim3 grid(1, (seq_len + BLOCK_M - 1) / BLOCK_M, batch_size * num_heads);
+    dim3 block(128);
+
+    int hd_stride = head_dim + 1;
+    int sn_stride = BLOCK_N + 1;
+    size_t smem_size = (BLOCK_M * hd_stride + 4 * BLOCK_N * hd_stride  // double buffered K/V
+                        + BLOCK_M * sn_stride + BLOCK_M * head_dim + 3 * BLOCK_M) *
+                       sizeof(float);
+
+    int device;
+    CUDA_CHECK (cudaGetDevice(&device))
+        ;
+    int max_smem;
+    CUDA_CHECK (cudaDeviceGetAttribute(&max_smem, cudaDevAttrMaxSharedMemoryPerBlock, device))
+        ;
+    if (static_cast<int>(smem_size) > max_smem) {
+        throw std::runtime_error("flash_attention: head_dim=" + std::to_string(head_dim) +
+                                 " requires " + std::to_string(smem_size) +
+                                 " bytes shared memory, but device max is " +
+                                 std::to_string(max_smem) + " bytes.");
+    }
+
+    flash_attention_forward_kernel<__nv_bfloat16, BLOCK_M, BLOCK_N><<<grid, block, smem_size, stream>>>(
+        Q, K, V, O, batch_size, num_heads, seq_len, head_dim, scale, is_causal);
+    CUDA_CHECK (cudaGetLastError())
+        ;
+}
diff --git a/src/hgemm_kernel.cu b/src/hgemm_kernel.cu
index ea33494..17593fd 100644
--- a/src/hgemm_kernel.cu
+++ b/src/hgemm_kernel.cu
@@ -1,3 +1,4 @@
+#include <cuda_bf16.h>
 #include <mma.h>
 
 #include "common.cuh"
@@ -219,6 +220,18 @@ template void hgemm_impl<half>(const half*,
                                MatrixLayout,
                                cudaStream_t);
 
+template void hgemm_impl<__nv_bfloat16>(const __nv_bfloat16*,
+                                        const __nv_bfloat16*,
+                                        __nv_bfloat16*,
+                                        int,
+                                        int,
+                                        int,
+                                        float,
+                                        float,
+                                        MatrixLayout,
+                                        MatrixLayout,
+                                        cudaStream_t);
+
 // Host wrapper functions
 void hgemm_fp32(const float* A,
                 const float* B,
@@ -248,6 +261,20 @@ void hgemm_fp16(const half* A,
     hgemm_impl<half>(A, B, C, M, N, K, alpha, beta, layout_a, layout_b, stream);
 }
 
+void hgemm_bf16(const __nv_bfloat16* A,
+                const __nv_bfloat16* B,
+                __nv_bfloat16* C,
+                int M,
+                int N,
+                int K,
+                float alpha,
+                float beta,
+                MatrixLayout layout_a,
+                MatrixLayout layout_b,
+                cudaStream_t stream) {
+    hgemm_impl<__nv_bfloat16>(A, B, C, M, N, K, alpha, beta, layout_a, layout_b, stream);
+}
+
 // Convenience wrappers for different transpose combinations
 void gemm_nn(const float* A,
              const float* B,
diff --git a/src/naive_attention.cu b/src/naive_attention.cu
index 8714e1c..28cad9b 100644
--- a/src/naive_attention.cu
+++ b/src/naive_attention.cu
@@ -1,6 +1,9 @@
 #include <cfloat>
 #include <cmath>
 
+#include <cuda_bf16.h>
+#include <cuda_fp16.h>
+
 #include "common.cuh"
 #include "warp_primitives.cuh"
 
@@ -171,3 +174,39 @@ void naive_attention_fp16(const half* Q,
     CUDA_CHECK (cudaGetLastError())
         ;
 }
+
+void naive_attention_bf16(const __nv_bfloat16* Q,
+                          const __nv_bfloat16* K,
+                          const __nv_bfloat16* V,
+                          __nv_bfloat16* O,
+                          int batch_size,
+                          int num_heads,
+                          int seq_len,
+                          int head_dim,
+                          float scale,
+                          bool is_causal,
+                          cudaStream_t stream) {
+    dim3 grid(1, seq_len, batch_size * num_heads);
+    constexpr int BLOCK_THREADS = 256;
+    constexpr int REDUCE_SMEM = BLOCK_THREADS / 32;
+    dim3 block(BLOCK_THREADS);
+    size_t smem_size = (seq_len + REDUCE_SMEM) * sizeof(float);
+
+    int device;
+    CUDA_CHECK (cudaGetDevice(&device))
+        ;
+    int max_smem;
+    CUDA_CHECK (cudaDeviceGetAttribute(&max_smem, cudaDevAttrMaxSharedMemoryPerBlock, device))
+        ;
+    if (static_cast<int>(smem_size) > max_smem) {
+        throw std::runtime_error(
+            "naive_attention: seq_len=" + std::to_string(seq_len) + " requires " +
+            std::to_string(smem_size) + " bytes shared memory, but device max is " +
+            std::to_string(max_smem) + " bytes. Use tiled or flash attention for long sequences.");
+    }
+
+    naive_attention_simple_kernel<__nv_bfloat16><<<grid, block, smem_size, stream>>>(
+        Q, K, V, O, batch_size, num_heads, seq_len, head_dim, scale, is_causal);
+    CUDA_CHECK (cudaGetLastError())
+        ;
+}
diff --git a/src/tensor_core_gemm.cu b/src/tensor_core_gemm.cu
index d7a1102..1a2425a 100644
--- a/src/tensor_core_gemm.cu
+++ b/src/tensor_core_gemm.cu
@@ -1,3 +1,4 @@
+#include <cuda_bf16.h>
 #include <mma.h>
 
 #include "common.cuh"
diff --git a/src/tiled_attention.cu b/src/tiled_attention.cu
index 58f0800..68e7cee 100644
--- a/src/tiled_attention.cu
+++ b/src/tiled_attention.cu
@@ -1,5 +1,8 @@
 #include <cfloat>
 
+#include <cuda_bf16.h>
+#include <cuda_fp16.h>
+
 #include "common.cuh"
 #include "shared_memory.cuh"
 #include "warp_primitives.cuh"
@@ -288,3 +291,45 @@ void tiled_attention_fp16(const half* Q,
     CUDA_CHECK (cudaGetLastError())
         ;
 }
+
+void tiled_attention_bf16(const __nv_bfloat16* Q,
+                          const __nv_bfloat16* K,
+                          const __nv_bfloat16* V,
+                          __nv_bfloat16* O,
+                          int batch_size,
+                          int num_heads,
+                          int seq_len,
+                          int head_dim,
+                          float scale,
+                          bool is_causal,
+                          cudaStream_t stream) {
+    constexpr int BLOCK_M = 32;
+    constexpr int BLOCK_N = 32;
+
+    dim3 grid(1, (seq_len + BLOCK_M - 1) / BLOCK_M, batch_size * num_heads);
+    dim3 block(256);
+
+    int hd_stride = head_dim + 1;
+    int sn_stride = BLOCK_N + 1;
+    size_t smem_size =
+        (BLOCK_M * hd_stride + 2 * BLOCK_N * hd_stride + BLOCK_M * sn_stride + BLOCK_M * head_dim) *
+        sizeof(float);
+
+    int device;
+    CUDA_CHECK (cudaGetDevice(&device))
+        ;
+    int max_smem;
+    CUDA_CHECK (cudaDeviceGetAttribute(&max_smem, cudaDevAttrMaxSharedMemoryPerBlock, device))
+        ;
+    if (static_cast<int>(smem_size) > max_smem) {
+        throw std::runtime_error("tiled_attention: head_dim=" + std::to_string(head_dim) +
+                                 " requires " + std::to_string(smem_size) +
+                                 " bytes shared memory, but device max is " +
+                                 std::to_string(max_smem) + " bytes.");
+    }
+
+    tiled_attention_kernel<__nv_bfloat16, BLOCK_M, BLOCK_N><<<grid, block, smem_size, stream>>>(
+        Q, K, V, O, batch_size, num_heads, seq_len, head_dim, scale, is_causal);
+    CUDA_CHECK (cudaGetLastError())
+        ;
+}
diff --git a/tests/test_attention.py b/tests/test_attention.py
index 1dbc451..c4bfad2 100644
--- a/tests/test_attention.py
+++ b/tests/test_attention.py
@@ -24,6 +24,13 @@ batch_strategy = st.integers(min_value=1, max_value=4)
 head_strategy = st.integers(min_value=1, max_value=8)
 seq_len_strategy = st.integers(min_value=16, max_value=256)
 head_dim_strategy = st.sampled_from([32, 64, 128])
+attention_op_names = ("naive_attention", "tiled_attention", "flash_attention")
+
+
+def _bf16_attention_supported(device):
+    """Return whether the current CUDA device supports native BF16 attention."""
+    major, _ = torch.cuda.get_device_capability(device)
+    return major >= 8
 
 
 class TestNaiveAttention:
@@ -320,6 +327,59 @@ class TestTiledAttention:
             msg="Tiled attention FP16 correctness failed",
         )
 
+
+class TestBF16Attention:
+    """Focused BF16 coverage for attention APIs."""
+
+    @pytest.mark.cuda
+    @pytest.mark.parametrize("op_name", attention_op_names)
+    def test_attention_correctness_bf16(self, op_name, device):
+        """BF16 attention should match a float32 PyTorch reference on SM80+."""
+        try:
+            import cuda_llm_ops
+        except ImportError:
+            pytest.skip("CUDA kernels not built")
+
+        if not _bf16_attention_supported(device):
+            pytest.skip("BF16 attention requires SM80+ hardware")
+
+        op = getattr(cuda_llm_ops, op_name)
+        q = torch.randn(2, 4, 64, 32, device=device, dtype=torch.bfloat16)
+        k = torch.randn_like(q)
+        v = torch.randn_like(q)
+
+        output = op(q, k, v, is_causal=True)
+        reference, _ = compute_attention_reference(q.float(), k.float(), v.float(), is_causal=True)
+
+        assert output.dtype == torch.bfloat16
+        assert_close(
+            output.float(),
+            reference.float(),
+            rtol=2e-2,
+            atol=2e-2,
+            msg=f"{op_name} BF16 correctness failed",
+        )
+
+    @pytest.mark.cuda
+    @pytest.mark.parametrize("op_name", attention_op_names)
+    def test_attention_bf16_requires_sm80(self, op_name, device):
+        """BF16 attention should raise a clear error on unsupported hardware."""
+        try:
+            import cuda_llm_ops
+        except ImportError:
+            pytest.skip("CUDA kernels not built")
+
+        if _bf16_attention_supported(device):
+            pytest.skip("BF16 attention is supported on this GPU")
+
+        op = getattr(cuda_llm_ops, op_name)
+        q = torch.randn(2, 4, 64, 32, device=device, dtype=torch.bfloat16)
+        k = torch.randn_like(q)
+        v = torch.randn_like(q)
+
+        with pytest.raises(RuntimeError, match="SM80\\+.*bfloat16|bfloat16.*SM80\\+"):
+            op(q, k, v)
+
     @pytest.mark.cuda
     def test_tiled_attention_scale_parameter(self, device):
         """Test tiled_attention with custom scale parameter."""
@@ -421,7 +481,7 @@ class TestNaiveAttentionErrorHandling:
         k = torch.randn_like(q)
         v = torch.randn_like(q)
 
-        with pytest.raises(RuntimeError, match="Only float32 and float16"):
+        with pytest.raises(RuntimeError, match="Only float32, float16, and bfloat16"):
             naive_attention(q, k, v)
 
     @pytest.mark.cuda
@@ -528,7 +588,7 @@ class TestTiledAttentionErrorHandling:
         k = torch.randn_like(q)
         v = torch.randn_like(q)
 
-        with pytest.raises(RuntimeError, match="Only float32 and float16"):
+        with pytest.raises(RuntimeError, match="Only float32, float16, and bfloat16"):
             tiled_attention(q, k, v)
 
     @pytest.mark.cuda
diff --git a/tests/test_interface.py b/tests/test_interface.py
index 522b10e..6415a51 100644
--- a/tests/test_interface.py
+++ b/tests/test_interface.py
@@ -20,6 +20,15 @@ except ImportError:
     from tests.conftest import assert_close
 
 
+attention_op_names = ("naive_attention", "tiled_attention", "flash_attention")
+
+
+def _bf16_attention_supported(device):
+    """Return whether the current CUDA device supports native BF16 attention."""
+    major, _ = torch.cuda.get_device_capability(device)
+    return major >= 8
+
+
 class TestPythonInterface:
     """Tests for Python interface compatibility."""
 
@@ -64,6 +73,29 @@ class TestPythonInterface:
             # Check dtype preserved
             assert output.dtype == q.dtype, f"Dtype mismatch: {output.dtype} vs {q.dtype}"
 
+    @pytest.mark.cuda
+    @pytest.mark.parametrize("op_name", attention_op_names)
+    def test_attention_bf16_tensor_compatibility(self, op_name, device):
+        """Attention APIs should accept BF16 tensors and preserve dtype on SM80+."""
+        try:
+            import cuda_llm_ops
+        except ImportError:
+            pytest.skip("CUDA kernels not built")
+
+        if not _bf16_attention_supported(device):
+            pytest.skip("BF16 attention requires SM80+ hardware")
+
+        op = getattr(cuda_llm_ops, op_name)
+        q = torch.randn(2, 4, 64, 32, device=device, dtype=torch.bfloat16)
+        k = torch.randn_like(q)
+        v = torch.randn_like(q)
+
+        output = op(q, k, v)
+
+        assert isinstance(output, torch.Tensor)
+        assert output.device == q.device
+        assert output.dtype == torch.bfloat16
+
     @pytest.mark.cuda
     @pytest.mark.property
     @settings(max_examples=50, deadline=None)
@@ -176,6 +208,26 @@ class TestErrorHandling:
 
         assert "dtype" in str(excinfo.value).lower() or "type" in str(excinfo.value).lower()
 
+    @pytest.mark.cuda
+    @pytest.mark.parametrize("op_name", attention_op_names)
+    def test_attention_bf16_requires_sm80_error(self, op_name, device):
+        """BF16 attention should fail with a clear runtime error on pre-SM80 GPUs."""
+        try:
+            import cuda_llm_ops
+        except ImportError:
+            pytest.skip("CUDA kernels not built")
+
+        if _bf16_attention_supported(device):
+            pytest.skip("BF16 attention is supported on this GPU")
+
+        op = getattr(cuda_llm_ops, op_name)
+        q = torch.randn(2, 4, 64, 32, device=device, dtype=torch.bfloat16)
+        k = torch.randn_like(q)
+        v = torch.randn_like(q)
+
+        with pytest.raises(RuntimeError, match="SM80\\+.*bfloat16|bfloat16.*SM80\\+"):
+            op(q, k, v)
+
     @pytest.mark.cuda
     def test_gemm_dimension_mismatch(self, device):
         """Test GEMM dimension mismatch error."""
